spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    username: will
    password: password
    url: jdbc:postgresql://localhost:5432/docdex
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true